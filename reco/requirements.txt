# Core service
fastapi==0.115.0
uvicorn[standard]==0.30.6
pymongo==4.8.0
pydantic==2.9.2
python-dotenv==1.0.1

# L1 association-rule mining (offline job)
pandas==2.2.2
mlxtend==0.23.1

# L5 LLM rerank + copy
# >=1.55.3 required: older versions pass `proxies` to httpx, removed in httpx 0.28
openai>=1.55.3,<2
# Use the OS certificate store (fixes corp TLS-inspection connection errors)
truststore>=0.9

# L3 embeddings / vector search (optional — guarded imports)
fastembed==0.3.6
qdrant-client==1.11.2

# P4 Zalo QR payment (scannable QR image for the simulated pay flow)
qrcode[pil]==7.4.2
