Drop kiosk marketing images here (optional — the UI has styled fallbacks if missing):

  welcome-hero.png   1080 x 1400 px   Welcome-screen promo graphic (KFC Rewards registration block).
  menu-banner.png    1080 x 264  px   Wide banner across the top of the menu page.

PNG or JPG (rename to .png). Product photos are pulled live from KFC's CDN, so you
do NOT need to generate those.
